// Import data from a CSV file into marketData table
importData: {
    data: ("DFFFIF"; ",") 0:`$x; // Define column format and load CSV
    marketData insert data
    count marketData
}

// Example usage
importData["data/raw/market_data.csv"]
